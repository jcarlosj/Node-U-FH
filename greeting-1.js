// Primero en Ejecutarse
function greeting( name ) {
    return `Hola ${ name }`;
}

let saludo = greeting( 'Elisa Maria' );     // Segundo en Ejecutarse
console .log( saludo );                     // Tercero en Ejecutarse
